apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: opensearch
  namespace: flux-system
spec:
  interval: 1m
  path: "./infrastructure/monitoring/opensearch/conf"
  prune: true
  sourceRef:
    kind: GitRepository
    name: infrastructure-repo
